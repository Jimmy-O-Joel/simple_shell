#include "shell.h"

/**
 * exit_shell - exits the shell
 *
 * @datash: data relevant (status and args)
 * Return: 0 on success.
 */
 
int exit_shell(__attribute__((unused))char **args, __attribute__((unused))char **_environ)
{
	return (0);
}
